DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

if [ "$@" = "update" ]; then
  exec "${DIR}/bundle/bin/bundle-env" gem update fastlane
else
  exec "${DIR}/bundle/bin/bundle-env" fastlane "$@"
fi;
